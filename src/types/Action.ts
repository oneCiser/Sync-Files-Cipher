export enum Action {
    COMPARE_ROLLINGS,
    STREAM_BUFFERS,
    PREPARE_STREAM,
    CLOSE_CONNECTION,
    STREAM_START,
    REMOVE_FILE,
    REMOVE_DIR,
    ADD_DIR,
    ADD_FILE,
    MOVE,
    ERROR
}

